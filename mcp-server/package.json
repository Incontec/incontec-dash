{
  "name": "incontec-mcp-financeiro",
  "version": "1.0.0",
  "description": "Servidor MCP que expoe os dados financeiros do Supabase (contas a pagar, a receber, kpis, bancos, fluxo de caixa) como ferramentas para o assistente de IA.",
  "type": "module",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
